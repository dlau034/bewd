<h2>This is the index page</h2>

<% @bicycle_orders.each do |f| %>

	<div class="module">
	  	
	    <p><%= link_to f.name, edit_bicycle_order_path(f.id) %></p> 
	    <p><%= f.name %></p>
	  </div>
	<% end %>	